rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads and writes require authentication (backoffice).
    // The frontoffice uses Firebase Admin SDK server-side, which bypasses these rules.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
